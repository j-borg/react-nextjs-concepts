import { Meta, StoryObj } from '@storybook/react'

import { UserView } from './UserView'

export default { component: UserView } as Meta

export const Primary: StoryObj = {
  args: {},
  argTypes: {}
}
